# /routes
get:
  summary: List all routes
  description: Returns all routes in the database as a GeoJSON Feature Collection. Each feature has id, name, and type properties and a Point geometry with the route coordinate.
  operationId: getRoutes
  tags:
    - routes
  x-amazon-apigateway-integration:
    $ref: '../integrations/getRoutesLambda.yaml#/GetRoutesLambdaIntegration'
  responses:
    '200':
      description: Successful response; GeoJSON Feature Collection of routes
      content:
        application/json:
          schema:
            $ref: '../components/getRoutesResponse.yaml#/GetRoutesResponse'
          examples:
            getRoutesResponse:
              $ref: '../examples/getRoutesResponse.yaml'
    '500':
      description: Internal server error
