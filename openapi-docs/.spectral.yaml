extends:
  - spectral:oas
  - spectral-aws-apigateway-ruleset

rules:
  operation-tag-defined: off
  # Redocly bundle --dereferenced inlines all $refs; leftover components.* entries are not referenced.
  oas3-unused-component: off
