AWSTemplateFormatVersion: '2010-09-09'
Description: Lambda for GET /ropewiki/region/{id}/images API endpoint

Resources:
  GetRopewikiRegionImages:
    Type: 'AWS::Serverless::Function'
    Properties:
      Description: 'Lambda for GET /ropewiki/region/{id}/images API endpoint; returns paginated RopewikiRegionImageView (banner images) for pages in the region and its descendants'
      Runtime: nodejs22.x
      Architectures:
        - arm64
      Role:
        'Fn::GetAtt': GetRopewikiRegionImagesRole.Arn
      CodeUri: ../../
      Handler: src/api/getRopewikiRegionImages/handler.handler
      MemorySize: 256
      Timeout: 30
      VpcConfig:
        SecurityGroupIds:
          - Ref: LambdaSecurityGroup
        SubnetIds:
          - Ref: PrivateSubnet1
      Environment:
        Variables:
          DEV_ENVIRONMENT:
            Ref: devEnvironment
          DB_HOST:
            'Fn::If':
              - UseDatabaseProxyInProd
              - 'Fn::GetAtt': RDSProxy.Endpoint
              - 'Fn::GetAtt': Database.Endpoint.Address
          DB_PORT:
            'Fn::GetAtt': Database.Endpoint.Port
          USE_DATABASE_PROXY:
            Ref: UseDatabaseProxy
          DB_NAME:
            'Fn::Sub': '${devEnvironment}db'
          DB_USER:
            Ref: DatabaseUsername
          DB_PASSWORD:
            Ref: DatabasePassword
    Metadata:
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: esnext
        Sourcemap: true
        EntryPoints:
          - src/api/getRopewikiRegionImages/handler.ts
        External:
          - sharp
          - pdf-lib
          - pdf-to-img
Outputs:
  GetRopewikiRegionImagesLambdaArn:
    Description: ARN of the GetRopewikiRegionImages Lambda (for OpenAPI integration resolveYamlEnvs)
    Value:
      'Fn::GetAtt': GetRopewikiRegionImages.Arn
