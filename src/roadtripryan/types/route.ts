export class RoadTripRyanRoute {
    route: string
    roadTripRyanPage: string
    mapData: string

    constructor(
        route: string,
        roadTripRyanPage: string,
        mapData: string,
    ) {
        this.route = route;
        this.roadTripRyanPage = roadTripRyanPage;
        this.mapData = mapData;
    }
}